import { AccountSettingsView } from "@/components/modules/profile/account-settings-view";

/** Shared by every role, like /profile — nothing here is role-specific. */
export default function SettingsPage() {
  return <AccountSettingsView />;
}
