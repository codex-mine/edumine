fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.5
pydantic-settings==2.7.1
sqlalchemy[asyncio]==2.0.37
asyncpg==0.30.0
alembic==1.14.0
slowapi==0.1.9
python-dotenv==1.0.1
bcrypt==4.2.1
pyjwt==2.10.1
anthropic==0.69.0
python-multipart==0.0.20
cloudinary==1.45.0

# OMR sheet scanning. opencv is deliberately held on the 4.x line: the bubble
# detection thresholds are calibrated against it, and OpenCV 5 changes enough of
# the imaging stack that a major bump must be re-validated against
# tests/fixtures/omr rather than taken silently.
opencv-python-headless==4.11.0.86
numpy==2.2.1
openpyxl==3.1.5

pytest==9.1.1
pytest-asyncio==1.4.0
